function sayHi() {
    //everything in here gets executed everytime i call the function
    console.log('Hi')
    //we can have several lines here
}
//function saves code but doesnt do anything until called
//calling the function:
sayHi()
//says 'Hi'

function sum(a, b) {
    console.log(a + b)
}
sum(1, 2)
// equals 3, if we dont put a value, it says undefined